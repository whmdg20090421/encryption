<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- 文件操作前台服务 -->
        <service
            android:name="com.whmdg.mczj.tools.fileop.FileOperationService"
            android:label="文件操作"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 云盘同步前台服务（上传/下载保活） -->
        <service
            android:name="com.whmdg.mczj.tools.fileop.sync.CloudSyncForegroundService"
            android:label="云盘同步"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
